# james\_crowley\.intel\_amt Release Notes

**Topics**

- <a href="#v0-2-0">v0\.2\.0</a>
    - <a href="#release-summary">Release Summary</a>
    - <a href="#minor-changes">Minor Changes</a>
    - <a href="#bugfixes">Bugfixes</a>
- <a href="#v0-1-0">v0\.1\.0</a>
    - <a href="#release-summary-1">Release Summary</a>
    - <a href="#minor-changes-1">Minor Changes</a>
    - <a href="#breaking-changes--porting-guide">Breaking Changes / Porting Guide</a>
    - <a href="#security-fixes">Security Fixes</a>
    - <a href="#bugfixes-1">Bugfixes</a>
    - <a href="#new-modules">New Modules</a>

<a id="v0-2-0"></a>
## v0\.2\.0

<a id="release-summary"></a>
### Release Summary

Additive release\. Nothing in 0\.1\.0\'s return shapes changed\, so this is a drop\-in
upgrade\.

<code>amt\_info</code> gains network\, system\-state and BIOS facts\, most usefully the
endpoint\'s MAC address \-\- a second identity anchor alongside the platform GUID\,
and the key a PXE reservation is made against \-\- and <code>wake\_on\_lan\_capable</code>\,
which explains a failure mode that previously looked like a network fault\: an
endpoint whose <code>LinkPolicy</code> omits \"network link always on\" does not answer
WS\-Man while powered off\, so <code>amt\_power</code> with <code>state\=on</code> cannot reach it\.

<code>amt\_power</code> gains <code>sleep\-light</code>\, <code>sleep\-deep</code> and <code>hibernate</code>\. These were
already implemented with the correct CIM codes but were unreachable\, because the
module\'s <code>state</code> choices omitted them\.

These additions came out of an audit of <code>parmstro/intel\_amt</code> \(GPL\-3\.0\-or\-later\,
reuse authorised and attributed in <code>NOTICE</code>\)\. What was taken is protocol facts
from that project\'s hardware research notes \-\- property names and values dumped
from a real AMT 10\.0\.56 machine \-\- and not code\. They are therefore recorded as
Tier 2 in <code>docs/capability\-matrix\.md</code>\: mock\-tested here\, resting on someone
else\'s hardware\, and <strong>unverified on AMT 16\.1\.30 or 19\.0\.5</strong>\, the two generations
every hardware\-qualified fact in 0\.1\.0 rests on\. The same applies to the three new
power actions\, which additionally depend on target\-OS ACPI support\.

Deliberately not adopted from that audit\: its network\-settings\, TLS\-config and
user modules\. <code>docs/capability\-matrix\.md</code> records why\, and the short version is
that each is either a non\-functional stub or capable of stranding the management
path in a way only a physical MEBx visit recovers\.

<a id="minor-changes"></a>
### Minor Changes

* amt\_info \- derive C\(amt\.network\.wake\_on\_lan\_capable\) from whether C\(LinkPolicy\) contains V\(16\) \(network link always on\)\, and report C\(link\_policy\_names\) decoded alongside the raw list\. An endpoint without V\(16\) does not answer WS\-Man while powered off\, so C\(amt\_power\) with C\(state\=on\) fails there looking exactly like a network fault\; surfacing it read\-only turns that into a diagnosis\. V\(null\)\, not V\(false\)\, when C\(LinkPolicy\) is absent\.
* amt\_info \- normalize C\(amt\.network\.mac\_address\) to colon\-separated lowercase and keep the firmware\'s own formatting in C\(amt\.network\.mac\_address\_raw\)\. Real AMT 10 firmware returns the MAC dash\-separated while the research notes\' own sample claims colons\, and the MAC is both a second identity anchor alongside the platform GUID and the key a PXE reservation is made against \-\- comparisons a stray separator silently breaks\.
* amt\_info \- report C\(AMT\_EthernetPortSettings\) instance 0 under a new C\(amt\.network\) key\: MAC address\, IPv4 address/mask/gateway/DNS\, C\(dhcp\_enabled\)\, C\(link\_is\_up\)\, C\(ip\_sync\_enabled\) and the raw C\(LinkPolicy\) list\. Read with C\(Get\) and an exact C\(InstanceID\) selector\, never C\(Enumerate\)\. Instance 0 only\; a firmware with no such instance reports V\(null\) rather than failing\.
* amt\_info \- report C\(CIM\_ComputerSystem\) state under a new C\(amt\.system\_state\) key\: C\(ElementName\)\, C\(EnabledState\) and C\(OperationalStatus\) decoded against the full DMTF tables\, and C\(RequestedState\) raw\. C\(OperationalStatus\) is always a list because CIM types it as an array\. This reintroduces a WS\-Man round trip that 0\.1\.0 removed\; the class now sources three useful state fields rather than a C\(UUID\) property it does not have\. C\(amt\.uuid\) still comes from C\(CIM\_ComputerSystemPackage\.PlatformGUID\)\.
* amt\_info \- report the host BIOS version from C\(CIM\_BIOSElement\.Version\) as C\(amt\.bios\_version\)\, falling back to C\(Enumerate\) when the bare C\(Get\) faults\. This is the least well\-evidenced fact the module returns and is expected to be V\(null\) on some firmware\; see C\(docs/capability\-matrix\.md\)\.
* amt\_info \- report the rest of the C\(AMT\_GeneralSettings\) instance already being read for the hostname\: C\(domain\_name\)\, C\(idle\_wake\_timeout\)\, C\(ping\_response\_enabled\)\, C\(rmcp\_ping\_response\_enabled\)\, C\(network\_interface\_enabled\) and C\(ddns\_update\_enabled\)\. Zero extra WS\-Man round trips\. C\(PowerSource\) and C\(PrivacyLevel\) are deliberately not surfaced\: nothing documents what their integers mean\.
* amt\_info \- the module documentation now states the WS\-Man round\-trip cost explicitly\: ten HTTP requests\, of which three C\(Get\)s are new\. All new facts are additive \-\- every pre\-existing C\(amt\) key keeps its name\, position and shape\, which a new test asserts\.
* amt\_power \- expose the V\(sleep\-light\)\, V\(sleep\-deep\) and V\(hibernate\) power actions\, which were fully implemented in C\(module\_utils/client\.py\) \-\- correct CIM C\(RequestPowerStateChange\) codes 3\, 4 and 7\, and correct expected\-state mappings \-\- but omitted from the module\'s C\(state\) choices\, so they were unreachable dead code\. V\(hibernate\) is convergent\; the two sleep depths deliberately are not\, because CIM 3 \(S1\) and CIM 4 \(S3\) both normalize to the single state C\(sleep\)\, so treating them as convergent would report C\(changed\=false\) for a transition that was never issued\. All three are unverified against real firmware and additionally depend on target\-OS ACPI support\, which the documentation now states\.
* docs/protocol\-notes\.md \- new section 2\.7 recording the network and system\-state classes\, their selectors and their property tables\, plus the hardware\-verified finding that C\(Enumerate\) returns HTTP 400 on AMT 10 for C\(AMT\_EthernetPortSettings\)\, C\(AMT\_GeneralSettings\)\, C\(AMT\_BootCapabilities\)\, C\(AMT\_BootSettingData\) and C\(AMT\_TLSSettingData\) while C\(Get\) with an exact selector works\. This cuts against the enumerate\-first habit elsewhere in the client\, so the existing hardware\-verified C\(Enumerate\) call sites are left alone rather than swapped for an unverified verb\. Derived from parmstro/intel\_amt\'s hardware research notes\, with attribution recorded per file in C\(NOTICE\)\; protocol facts were taken\, no code was\.
* tests \- the mock WS\-Man server answers the three new reads the way real firmware does\: it requires the exact C\(Get\) selector for C\(AMT\_EthernetPortSettings\) and faults without it\, serves the MAC dash\-separated\, renders C\(LinkPolicy\) and C\(OperationalStatus\) as repeated elements\, and can be started with C\(\-\-no\-ethernet\-port\) or C\(\-\-bios\-get\-faults\) so the integration target exercises graceful degradation and the C\(Enumerate\) fallback over a real socket rather than only where a unit test mocks the transport away\.

<a id="bugfixes"></a>
### Bugfixes

* amt\_power \- a new test asserts that every C\(state\) choice except V\(query\) has an entry in both the action map and the expected\-state map\, and that the module\'s copy of the expected\-state map agrees with the client\'s\. The first guards the defect being fixed here \-\- three actions implemented but not reachable \-\- and the second guards check mode previewing a different outcome than a real run would produce\.

<a id="v0-1-0"></a>
## v0\.1\.0

<a id="release-summary-1"></a>
### Release Summary

First release\. Manages Intel AMT / vPro endpoints from Ansible\: capability
discovery\, power control\, one\-time boot selection\, redirection\-service state\,
and native IDE\-R virtual media — a bootable ISO and a writable image served to a
machine\'s firmware without shelling out to MeshCmd or Node\.js\.

Hardware\-qualified\, with the coverage recorded precisely because it is uneven\: all eight
stages passed on one machine running AMT 16\.1\.30 \-\- read\-only facts\, an
identity cross\-check\, check\-mode plans\, power on/off\, IDE\-R media attach and
boot\, a writable image\, native one\-time PXE\, and an idempotent re\-probe \-\-
while a second machine running AMT 19\.0\.5 reproduced only the three
non\-mutating stages\. Power\, media\, the writable\-image path and PXE are
therefore a single\-machine\, single\-firmware\-generation result\.

See <code>docs/capability\-matrix\.md</code> for what is verified against real firmware\,
what is only covered by mock fixtures\, and what remains unproven\. That
distinction is maintained deliberately\, and the \"still unproven\" list is short
and specific rather than absent\.

<a id="minor-changes-1"></a>
### Minor Changes

* Add C\(CONTRIBUTING\.md\) documenting the local verification sequence\, the C\(scripts/setup\-collection\-tree\.sh\) staging requirement \(it copies rather than symlinks\, and C\(COLLECTION\_BUILD\_ROOT\) must be set when several checkouts stage concurrently\)\, conventional commits\, the changelog\-fragment requirement\, and the two gates in front of hardware\-in\-the\-loop tests \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10)\)\.
* Add C\(docs/capability\-matrix\.md\)\, distinguishing what is verified against an authoritative firmware/reference source\, what is only unit/mock tested\, and what has not been verified against real firmware at all\, plus a firmware\-generation compatibility table marking inferred rows and the known open risks \(issue number 13\'s WS\-Addressing EPR byte\-form ambiguity\, the reset\-during\-write IDE\-R deferral gap\, Small Business Mode\'s hardware\-verified lack of TLS\, and C\(amt\_media\)\'s C\(validate\_certs\)/C\(ca\_path\) divergence from the WS\-Man modules\) \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10)\)\.
* Add CODE\_OF\_CONDUCT\.md referencing the Ansible Community Code of Conduct\, and \.github/CODEOWNERS marking the wire\-protocol and hardware\-touching paths\.
* Add GitHub issue forms for bug reports and feature requests\. The bug form asks for the fields that actually make a report actionable \-\- <code>error\_class</code>\, AMT firmware version\, and transport mode \-\- since behaviour genuinely differs across firmware generations\.
* Add SECURITY\.md with a private reporting channel\, and a plain explanation of why this collection warrants unusual care \-\- AMT credentials are equivalent to physical access\, and writable IDE\-R hands a remote BIOS raw block access to a local file\.
* Add <code>AmtClient</code>\, the typed facts/power management API that <code>amt\_info</code> and <code>amt\_power</code> adapt to\, built on the existing <code>WsmanClient</code> transport \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2)\, [https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/3](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/3)\)\.
* Add a pull request template whose checklist items each correspond to a class of bug that has really occurred in this collection\, including running sanity against the oldest supported ansible\-core rather than only the newest\.
* Add docs/maintainer\-setup\.md documenting every one\-time human step\: claiming a Galaxy namespace\, creating the restricted publish context\, installing Renovate\, and the public\-versus\-private trade\-off\.
* Add integration test targets for amt\_info\, amt\_power\, amt\_boot\, amt\_redirection\, and amt\_media\, driving the real modules against the mock WS\-Man and IDE\-R fixture servers over both plaintext and TLS with a pinned fingerprint\, including idempotence\, check\-mode\, a per\-module fault\-injection case\, and \(for amt\_media\) a multi\-frame write verified byte\-for\-byte on disk \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/8](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/8)\)\.
* Add per\-module reference pages under C\(docs/\) for M\(james\_crowley\.intel\_amt\.amt\_info\)\, M\(james\_crowley\.intel\_amt\.amt\_power\)\, M\(james\_crowley\.intel\_amt\.amt\_boot\)\, M\(james\_crowley\.intel\_amt\.amt\_redirection\)\, and M\(james\_crowley\.intel\_amt\.amt\_media\)\, each covering options\, return values\, examples\, idempotence/check\-mode behaviour\, error classes\, and known limitations\, verified against the argument specs and rendered C\(ansible\-doc\) output rather than written from memory \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10)\)\.
* Add renovate\.json covering the dependency surfaces Renovate can actually see\: pip requirements\, <code>cimg/python</code> CI images\, and the single <code>ansible\-core</code> pin used by the hardware job\.
* Add the <code>amt\_info</code> module \-\- read\-only capability and state discovery\. Capabilities \(<code>power</code>\, <code>boot\_once\_pxe</code>\, <code>sol</code>\, <code>storage\_redirection</code>\) and redirection\-service status are discovered from live firmware responses\; a firmware that omits an optional WS\-Man class degrades that capability to <code>false</code>/unknown instead of failing the whole read \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2)\)\.
* Add the <code>amt\_power</code> module \-\- convergent <code>on</code>/<code>off</code>\, imperative <code>reboot</code>/<code>reset</code>/<code>cycle</code>\, and a non\-mutating <code>query</code> state\. A successful <code>RequestPowerStateChange</code> is polled a bounded\, injectable number of times afterwards and reports what was actually observed\, since <code>ReturnValue \=\= 0</code> means accepted\, not complete \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/3](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/3)\)\.
* CI \- a weekly scheduled pipeline now runs the standard test workflow against main\, to catch breakage that arrives without a push such as a new ansible\-core point release\, a PyPI dependency change\, or a rebuilt CI image\. It cannot reach lab hardware\, because the hardware workflow requires a pipeline parameter that defaults to false\.
* CI \- add Python 3\.10 to the unit\-test matrix\, paired with ansible\-core 2\.17\. The matrix previously started at 3\.11 and was therefore blind to 3\.11\-only APIs on a version of Python the collection claims to support\.
* CI \- add a <code>hardware\-observe</code> job that reports the SHA\-256 leaf\-certificate fingerprint each AMT port offers\. It sends no credentials and mutates nothing\, so it is safe to run before an endpoint\'s identity is established\, and it produces the reviewed pin the credentialed stages require\.
* CI \- build the lab runner\'s qualification virtualenv from an explicitly versioned Python interpreter instead of bare <code>python3</code>\, which on that host is still the system Python 3\.10 and cannot run ansible\-core 2\.18 at all\. The command prefers <code>python3\.12</code>\, falls back to <code>python3\.11</code>/<code>python3\.13</code>\, and fails immediately \-\- naming what it looked for and what it found \-\- when none of them exists\, rather than letting the problem surface forty lines later as a pip resolution error nobody can debug without logging into the host\. It echoes the interpreter it chose and that interpreter\'s version\, so every job log records which one actually ran\. 3\.12 rather than the newest available Python for two reasons\: ansible\-core 2\.18\'s supported controller range ends at 3\.13\, and 3\.12 is also the newest version that can still run ansible\-core 2\.17\, the collection\'s declared floor\.
* CI \- hardware\-in\-the\-loop jobs target the <code>crowley/amt\-runner</code> self\-hosted resource class and run under the <code>amt\-lab\-runner</code> context\, which is restricted to this project alone so no other project in the organisation can borrow the lab credentials\. Four independent controls now stand in front of the runner \- the <code>run\-hardware\-tests</code> pipeline parameter\, a manual approval\, the project\-restricted context\, and a main\-branch filter\.
* CI \- lab inventory is rendered on the runner from context environment variables by <code>tests/hardware/render\-inventory\.sh</code> and removed afterwards\, so endpoint addresses\, credentials and reviewed TLS fingerprints never enter the repository\.
* CI \- the hardware job builds and installs the collection from the checkout before running playbooks\, so qualification runs against the code under test rather than a published release\.
* CI \- the hardware job installs ansible\-core into a workspace virtualenv rather than with <code>pip install \-\-user</code>\, because the lab runner is rootless with a read\-only root filesystem\.
* CI \- the hardware\-observe job now reports the certificate offered by every configured machine\, not just the first\, so a second machine\'s fingerprint can be reviewed before it is pinned\.
* CI \- wire hardware qualification stages 4\-7 \(<code>qualify\_power</code>\, <code>qualify\_media\_attach</code>\, <code>qualify\_writable\_image</code>\, <code>qualify\_pxe</code>\) into the <code>hardware</code> workflow as four new jobs \(<code>hardware\-power</code>\, <code>hardware\-media</code>\, <code>hardware\-writable</code>\, <code>hardware\-pxe</code>\)\, each escalating past the last and each behind its own independent <code>type\: approval</code> job\, so a human confirms every escalation separately rather than one approval covering everything from a power\-cycle through arming a native PXE boot\. All four keep the <code>amt\-lab\-runner</code> context and the main\-branch filter\. Stage 7\'s <code>amt\_pxe\_prereqs\_confirmed</code> attestation is wired to a new <code>pxe\-prereqs\-confirmed</code> pipeline parameter \(default <code>false</code>\)\, not hardcoded <code>true</code>\, so it has to be a fresh decision at trigger time\.
* De\-identify the lab from the repository now that it is public\. The platform GUID used as a byte\-order test vector is replaced with illustrative digits that preserve both properties the tests rely on\, and real AMT addresses and reviewed fingerprints are replaced with placeholders in maintainer documentation\.
* Documentation now records that the collection is hardware\-qualified against an Intel AMT 16\.1\.30 endpoint through all eight qualification stages\, and states specifically what remains unproven \-\- a non\-zero IDE\-R write\, whether a PXE exchange occurred\, AMT\'s internal one\-shot role bit\, a second machine\, and any other firmware generation\.
* Extend <code>AmtFacts</code> with <code>reported\_hostname</code>\, <code>capabilities</code>\, and <code>redirection</code> fields \(all firmware\-observed\, defaulted for backward compatibility\) and add the new <code>AmtCapabilities</code> dataclass\, so facts\-gathering has a typed home for capability discovery without collapsing it into an untyped dict \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/2)\)\.
* Integration test targets for all five modules\, driven against the deterministic mock WS\-Man and IDE\-R fixture servers\, including a case asserting that a multi\-frame write actually lands in the backing image \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20)\)\.
* Native Python IDE\-R engine\, removing the need to shell out to MeshCmd/Node\.js for virtual media\. Implements the redirection\-plane handshake\, RFC 2617 digest over the binary framing\, the IDE\-R state machine\, and SCSI target emulation \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14)\)\.
* TLS trust policy with two mutually exclusive modes \-\- CA chain plus hostname verification\, or exact SHA\-256 leaf\-certificate pinning implemented via urllib3\'s <code>assert\_fingerprint</code> so the pin authenticates the handshake itself \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12)\)\.
* Typed result models including CIM power\-state normalisation and the <code>intel\-amt\-operation/v1</code> operation receipt \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12)\)\.
* WS\-Man transport layer with SOAP envelope construction\, HTTP Digest authentication\, Enumerate/Pull paging\, and WS\-Addressing endpoint references \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12)\)\.
* Writable virtual media on the floppy/USB\-R device slot\, which the upstream reference implementation does not support\. The CD/DVD slot remains read\-only by design\, so a bootable ISO and a writable image can be attached in the same session \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14)\)\.
* amt\_boot \- new module that arms a one\-time boot device selection \(<code>pxe</code>\, <code>hdd</code>\, <code>cd</code>\, <code>bios</code>\, <code>ider\_floppy</code>\, <code>ider\_cdrom</code>\) using the exact five\-step <code>AMT\_BootSettingData</code> / <code>CIM\_BootConfigSetting</code> / <code>CIM\_BootService</code> sequence documented in this collection\'s protocol notes\, with mandatory discovery before any mutation and a required per\-call <code>action\_token</code> that is never re\-armed automatically \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13)\)\.
* amt\_media \- new module attaching and detaching IDE\-R virtual media\, so a bootable ISO and a writable answer\-file image can be served to a bare\-metal machine in one session \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20)\)\.
* amt\_media \- new module that attaches a bootable read\-only ISO and/or a writable answer image over IDE\-R\, and detaches a previously attached session\. An IDE\-R session is a long\-lived process\, so <code>state\=attached</code> forks a detached background daemon that owns the connection\, writes a small JSON state/receipt file keyed by <code>session\_id</code>\, and returns once that daemon has reported success or an early failure\; <code>state\=detached</code> looks the daemon up by the recorded pid and asks it to stop\. Credentials reach the daemon only as in\-memory arguments across the fork \-\- never via argv or the environment\, since no process is ever spawned or exec\'d \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7)\)\.
* amt\_redirection \- new module that reports Intel AMT redirection\-service support\, enablement\, and TCP reachability as three separate signals\, and optionally mutates enablement via <code>AMT\_RedirectionService\.RequestStateChange</code>\; read\-only by default and never attaches or streams media \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13)\)\.
* docs/capability\-matrix\.md gains a fourth tier for firmware\-verified claims and tabulates the six defects hardware qualification found that neither the unit nor the mock tier could have caught\.
* plugins/module\_utils/boot\.py \- data\-driven allow/deny\-list Put\-body construction for <code>AMT\_BootSettingData</code>\, deleting the fields newer firmware rejects when echoed back and zeroing <code>UefiBootNumberOfParams</code> when present \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13)\)\.
* plugins/module\_utils/media\_session\.py \- the detached\-session lifecycle this module builds on\: double\-fork daemonization\, an atomically\-written state file\, pid\-liveness\-based staleness recovery\, and pre\-spawn media validation shared between check mode and real attach \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7)\)\.
* plugins/module\_utils/redirection\.py \- add <code>RedirectionSession\.set\_recv\_timeout\(\)</code> \(so a long\-lived caller can wake periodically without busy\-polling\) and <code>peer\_certificate\_sha256\(\)</code> \(best\-effort TLS peer fingerprint\, mirroring <code>tls\.peer\_certificate\_evidence\(\)</code> for the WS\-Man transport\) \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/7)\)\.
* plugins/module\_utils/redirection\_service\.py \- WS\-Man view of redirection\-service capability/enablement plus an injectable TCP reachability probe\, kept separate from the stateful IDE\-R/SOL session transport \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/13)\)\.
* renovate\.json \- drop the <code>allowedVersions\: \'\<2\.18\'</code> cap on ansible\-core\. Its own documented removal condition\, the lab runner host being on Python 3\.11 or newer\, is now met\, so the cap no longer suppresses anything and the risk\-acceptance note explaining it has been removed rather than left behind as stale warning text\. Renovate will now raise 2\.18\+ updates for the hardware pin and list them on the Dependency Dashboard\.
* renovate\.json \- repair the custom regex manager for the hardware ansible\-core pin\. Its pattern assumed a trailing literal <code>\.0</code> \(<code>ansible\-core\~\=\(\?\<currentValue\>\\d\+\\\.\\d\+\)\\\.0</code>\) and so matched nothing once the pin became 2\.18\.18\, which would have left the only ansible\-core version Renovate can see silently unmanaged\, with no error anywhere to reveal it\.
* roles/amt\_baremetal\_install \- new reusable role that drives a complete unattended bare\-metal install lifecycle \(validate \-\> probe \-\> attach media \-\> arm one\-time boot \-\> reset \-\> observe \-\> wait for hand\-off \-\> detach\) on top of amt\_info\, amt\_power\, amt\_boot\, amt\_redirection\, and amt\_media\. Asserts the play targets exactly one host \(delegate\_to\: localhost does not stop inventory fan\-out\)\, requires an explicit confirm\_destructive\=true before doing anything destructive\, runs preflight capability checks via amt\_info before any mutation\, persists resumability state per target so an interrupted run never blindly re\-arms one\-time boot or re\-issues a reset of uncertain outcome\, and always detaches media via block/rescue/always even on failure\. Supports both an ider\_cdrom \(IDE\-R virtual media\) and a native pxe boot provider\, mutually exclusive by construction \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/9](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/9)\)\.
* tests/hardware \- add the eight\-stage hardware qualification playbooks \(qualify\_readonly\, qualify\_checkmode\, qualify\_power\, qualify\_media\_attach\, qualify\_writable\_image\, qualify\_pxe\, qualify\_idempotent\_reprobe\)\, an inventory\.yml\.example with obviously fake values\, and a README documenting the gating\, staged ordering rationale\, and the manual KVM/MEBx recovery path to keep reachable before running the media\-attach stage for the first time\. qualify\_readonly\.yml and qualify\_checkmode\.yml are the two filenames the CircleCI hardware workflow already references \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/11](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/11)\)\.
* tests/hardware \- every stage 4\-7 playbook \(<code>qualify\_power</code>\, <code>qualify\_media\_attach</code>\, <code>qualify\_writable\_image</code>\, <code>qualify\_pxe</code>\) now has the same block/rescue pattern <code>qualify\_readonly\.yml</code> already used\, surfacing <code>error\_class</code>/<code>endpoint</code>/<code>operation</code>/ <code>indeterminate</code> on failure instead of a <code>no\_log</code>\-censored\, undiagnosable one\. <code>roles/amt\_baremetal\_install</code>\'s own rescue \(used by stages 5 and 7\) does the same before re\-raising\, so a firmware failure inside the role is diagnosable too\. A new <code>amt\_qualify\_attended</code> variable \(default <code>true</code>\) gates every interactive <code>ansible\.builtin\.pause</code> in these playbooks\, so they can run unattended in CI \-\- where <code>pause</code> would otherwise block forever on stdin the machine executor cannot supply \-\- with the corresponding approval job acting as the human checkpoint instead\.
* tests/hardware \- support qualifying several machines\, each with its own credentials and its own reviewed TLS fingerprint\, via a numeric variable suffix \(<code>AMT\_HOST\_2</code>\, <code>AMT\_PASSWORD\_2</code>\, <code>AMT\_TLS\_FINGERPRINT\_2</code>\)\. Ports stay shared\, since AMT always listens on the same ones\. Per\-machine pins are required rather than optional\, because each endpoint presents its own self\-signed certificate and a shared fingerprint would surface as a pin mismatch rather than a configuration error\.
* tests/hardware/make\-test\-media\.sh \- new script that provisions the small bootable ISO \(iPXE\'s own <code>ipxe\.iso</code>\) and zero\-filled writable raw image stages 5 and 6 need\, entirely inside the workspace\. Idempotent and safe to re\-run\; fails loudly rather than producing a placeholder file if the download fails or a size check does not land on a multiple of 512 bytes\.
* tests/hardware/qualify\_pxe\.yml \- assert that <code>AMT\_BootSettingData</code> reads back unchanged after the one\-time PXE boot\'s reset\, in addition to confirming the arm reads back armed and the reset is issued and recovered\. Documented plainly\, in both the playbook and <code>tests/hardware/README\.md</code>\, that this does not read Intel AMT\'s internal one\-shot \(<code>IsNextSingleUse</code>\) role bit \-\- no module in this collection exposes a getter for it \-\- and that netboot succeeding is outside this playbook\'s authority to verify\, since it depends on DHCP/boot\-service infrastructure this collection cannot observe\.
* tests/hardware/qualify\_writable\_image\.yml \- weaken the \"bytes actually landed on disk\" assertion\. A BIOS sitting at a boot prompt does not spontaneously write to an attached floppy\, so <code>bytes\_written\=0</code> \(nothing booted on the target to issue a write\) is now reported as a legitimate\, expected outcome rather than a failure\. What is still asserted every run\: the image is accepted and attached\, and the device is presented to the guest as writable \(the MODE\_SENSE write\-protect bit is <code>0x00</code>\, not <code>0x80</code>\)\. A non\-zero <code>bytes\_written</code> is still cross\-checked against the on\-disk checksum\, since that combination disagreeing would be a real bug\.

<a id="breaking-changes--porting-guide"></a>
### Breaking Changes / Porting Guide

* amt\_boot\, amt\_redirection\, amt\_media \- the <code>intel\-amt\-operation/v1</code> receipt \(<code>schema</code>\, <code>action</code>\, <code>endpoint</code>\, <code>previous</code>\, <code>desired</code>\, <code>observed</code>\, <code>tls\_peer\_fingerprint</code>\, and the success\-path <code>error\_class</code>\) is now nested under an <code>operation</code> key on every module\, matching the shape <code>amt\_power</code> already used\. It was previously spread directly at the top level of these three modules\' results\, which meant a caller could not write one helper that reads <code>error\_class</code>/<code>tls\_peer\_fingerprint</code> from any module in the collection \-\- exactly the defect tracked as issue \#22\. Module\-specific return values \(for example <code>amt\_boot</code>\'s <code>device</code>/<code>boot\_config\_selector</code>/<code>boot\_source\_selector</code>\, <code>amt\_redirection</code>\'s <code>supported</code>/<code>enabled</code>/<code>transport\_reachable</code>\, and <code>amt\_media</code>\'s <code>session\_id</code>/<code>session\_state</code>/<code>devices</code>/<code>bytes\_read</code>/<code>bytes\_written</code>\) are unaffected and remain at the top level\. A failed module\'s result is unaffected by this change\: <code>error\_class</code> still surfaces at the top level of <code>fail\_json</code>\, since that is what Ansible itself surfaces and what rescue blocks read via <code>ansible\_failed\_result\.error\_class</code>\.
* amt\_info \- now returns a non\-secret <code>intel\-amt\-operation/v1</code> receipt under an <code>operation</code> key\, for parity with the other four modules\. It is read\-only \(<code>operation\.action</code> is always <code>get\_facts</code>\, <code>operation\.changed</code> is always <code>false</code>\)\, and <code>operation\.previous</code>/<code>operation\.desired</code>/<code>operation\.observed</code> are always <code>null</code> \-\- a read has no prior\, intended\, or separately\-observed state to report beyond what the existing <code>amt</code> return key already carries\.

<a id="security-fixes"></a>
### Security Fixes

* CI \- move the self\-hosted lab runner\'s hardware\-qualification virtualenv off ansible\-core 2\.17 and onto <code>ansible\-core\~\=2\.18\.18</code>\. The 2\.17 line is affected by GHSA\-w8p5\-mx5w\-cpqj \[HIGH\] \-\- argument injection in <code>ansible\-galaxy role install</code> leading to arbitrary code execution \-\- and that line is EOL\, so no 2\.17 release will ever carry the fix\. 2\.18\.18 is the first release that does\. The pin had been held at 2\.17 only because the runner host had nothing but Python 3\.10 and ansible\-core 2\.18\+ requires 3\.11\+ on the controller\; the host now has Python 3\.12\, so the blocker is gone rather than merely accepted\. Nothing changes for consumers \-\- <code>requires\_ansible</code> stays at <code>\>\=2\.17\.0</code>\, which is a floor and not a pin\.
* IDE\-R write payloads are bounded against the transfer length declared by the WRITE CDB\, and MediaImage refuses any write that would extend the backing file\. Previously a host could declare a small write\, pass the bounds check\, then stream unbounded DATA\_FROM\_HOST frames at an advancing offset and grow a local file \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14)\)\.
* The insecure\-transport gate is enforced in the <code>WsmanClient</code> constructor rather than only in the convenience classmethod\, so plaintext WS\-Man cannot be selected without explicitly setting <code>allow\_insecure\_transport\=true</code> regardless of which entry point is used \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/12)\)\.
* The redirection session refuses to downgrade to cleartext or cnonce\-less digest authentication\, and verifies a pinned TLS leaf certificate before transmitting any bytes \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14)\)\.
* amt\_media \- decide attach failure from whether the session daemon is still alive\, rather than from whether it managed to write an error state before the timeout\. Only an explicit error state used to fail the module\, so a daemon killed during the digest handshake \-\- which is exactly what bad credentials cause \-\- could fall through to a success receipt depending on timing\. A failed attach reporting success is the worst available outcome for a module whose job is to serve boot media \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/44](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/44)\)\.
* amt\_media \- require O\(tls\_fingerprint\) when O\(use\_tls\=true\)\, and reject O\(ca\_path\) outright\. The redirection plane is a raw TLS socket whose only trust mode is leaf pinning\; TLS without a pin is encrypted but unauthenticated\, and silently ignoring a supplied ca\_path would leave an operator believing the session was chain\-validated \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20)\)\.
* amt\_media \- session state files are created mode 0600 rather than at the umask default\, so they are not readable by other local users even when the runtime directory already exists with laxer permissions \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20)\)\.

<a id="bugfixes-1"></a>
### Bugfixes

* CI \- pin ansible\-core to 2\.17 in the hardware qualification job\. The lab runner image ships Python 3\.10 and ansible\-core 2\.19 requires 3\.11\+ on the controller\, so the job would have failed at dependency installation\. 2\.17 is also the collection\'s declared floor\, so hardware qualification now exercises the oldest version the collection claims to support\.
* CI \- the publish job reads <code>GALAXY\_API\_KEY</code>\, matching the name actually stored in the <code>galaxy\-publish</code> context\. It previously read <code>ANSIBLE\_GALAXY\_API\_KEY</code> and would have failed on its first real run\.
* Collection staging derives its build root from the repository path\, so concurrent checkouts on one host no longer clobber each other\'s staged tree \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/14)\)\.
* Exclude repository tooling from the built collection artifact\. <code>renovate\.json</code> was being shipped to consumers\; <code>CODE\_OF\_CONDUCT\.md</code> and <code>SECURITY\.md</code> are now excluded too\.
* Mock WS\-Man fixture server \- answer <code>Enumerate</code> for <code>AMT\_BootCapabilities</code>\, not only <code>Get</code>\. Unit tests mock enumeration directly\, so this was invisible until a real integration target sent a real <code>Enumerate</code> \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/pull/20)\)\.
* Omit WS\-Man method parameters whose value is <code>None</code> instead of emitting an empty element\. Real AMT 16\.1\.30 rejects <code>CIM\_BootConfigSetting\.ChangeBootOrder</code> carrying an empty <code>\<Source/\></code> with HTTP 400 and \"The supplied SOAP violates the corresponding XML schema definition\"\, because <code>Source</code> is typed as an endpoint reference and so requires child elements\. This blocked IDE\-R and BIOS boot entirely against real firmware\.
* README \- fix the unattended\-install example playbook referencing C\(media\.operation\.session\_id\)\, which does not exist\: M\(james\_crowley\.intel\_amt\.amt\_media\) returns RV\(session\_id\) at the top level of its result\, not nested under an C\(operation\) key \(that nesting is specific to M\(james\_crowley\.intel\_amt\.amt\_power\)\)\. Corrected to C\(media\.session\_id\) in both the boot\-arming and detach tasks \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/10)\)\.
* Support Python 3\.10 on the controller\, which the collection already claimed by declaring <code>requires\_ansible \'\>\=2\.17\.0\'</code> \(ansible\-core 2\.17 runs on 3\.10\) but did not actually honour\. <code>client\.py</code> imported <code>enum\.StrEnum</code> and <code>media\_session\.py</code> used <code>datetime\.UTC</code>\, both Python 3\.11\+\, so every module that imports the client raised ImportError on 3\.10 before any error handling could classify it\.
* <code>PowerAction</code> keeps <code>StrEnum</code> semantics via an explicit <code>\_\_str\_\_</code>\. A plain <code>\(str\, Enum\)</code> renders as <code>PowerAction\.ON</code> under <code>str\(\)</code> and f\-string interpolation rather than <code>on</code>\, which would have silently changed the shape of messages and receipt fields\.
* amt\_baremetal\_install \- <code>amt\_baremetal\_install\_host</code> no longer defaults to <code>inventory\_hostname</code>\. AMT answers on a separate management address from the operating system\'s hostname\, so that default silently pointed at the wrong endpoint or an unresolvable name\; it now resolves from <code>amt\_host</code> and otherwise stays unset so validation fails loudly\.
* amt\_baremetal\_install \- choose the power transition from the observed power state instead of always issuing <code>reset</code>\. A master bus reset does nothing to a machine that is already off\, so installing an operating system onto a powered\-off machine \-\- arguably the common bare\-metal case \-\- attached the media\, armed the one\-shot boot\, and then reported success having booted nothing\.
* amt\_baremetal\_install \- connection variables now fall back to the conventional <code>amt\_\*</code> inventory variables \(<code>amt\_host</code>\, <code>amt\_password</code>\, <code>amt\_tls\_fingerprint</code> and friends\) that this collection\'s README\, docs and example inventory all use\, so a conventional inventory drives the role with no per\-variable mapping\.
* amt\_info \- read the system UUID from <code>CIM\_ComputerSystemPackage\.PlatformGUID</code>\. It was read as <code>UUID</code> from <code>CIM\_ComputerSystem</code>\, which has no such property\, so it was null on every endpoint and silently disabled the identity cross\-check that guards against acting on the wrong machine \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/30](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/30)\)\.
* amt\_info \- render the system UUID in the canonical dashed form\, reversing the first three SMBIOS fields\. <code>PlatformGUID</code> arrives as 32 undashed hex characters with those fields little\-endian\, so the naive reading produces a plausible\-looking UUID that does not match what MEBx displays \-\- which would make the identity cross\-check fail against a correctly recorded expectation \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/32](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/32)\)\.
* amt\_info \- report the AMT firmware version from <code>CIM\_SoftwareIdentity</code> \(the instance whose <code>InstanceID</code> is <code>AMT</code>\, read from <code>VersionString</code>\)\. It was previously read from <code>AMT\_GeneralSettings\.Version</code> and <code>AMT\_SetupAndConfigurationService\.VersionsSupported</code>\, neither of which exists\, so the reported version was always null \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/18](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/18)\)\.
* amt\_info \- stop reading <code>CIM\_ComputerSystem</code> during facts gathering\. It existed only to source the UUID and contributed nothing else\, so removing it also saves a WS\-Man round trip per call\.
* amt\_power \- correct the documentation example\, which used <code>state\: on</code>\. YAML parses an unquoted <code>on</code> as the boolean <code>true</code> rather than the string <code>\"on\"</code>\, so anyone copying the example would have passed the wrong type and failed argument validation\.
* tests/hardware/qualify\_power\.yml \- restore the power state the stage started from\. It previously left the target powered off\, and the next stage issues a reset\, which does nothing to an off machine\.
* tests/hardware/qualify\_power\.yml \- restore the starting power state using <code>previous\_state</code>\, the key <code>amt\_power</code> actually returns\. It referenced a non\-existent <code>power\_state</code> key\, so the guard always evaluated to <code>unknown</code>\, the restore silently skipped on every run\, and the stage reported that it had left the machine as\-is\. An assertion now fails loudly if the starting state cannot be read\.
* tests/hardware/qualify\_readonly\.yml \- surface the redacted failure detail when amt\_info fails\. The task carries <code>no\_log\: true</code> to keep task arguments \(including the password\) out of the log\, but that also censored the module\'s own error\, which errors\.py has already redacted and which is therefore safe to show\. A censored failure is undiagnosable\, so a rescue now reports <code>error\_class</code>\, <code>endpoint</code>\, <code>operation</code>\, <code>indeterminate</code> and the redacted message\.
* tests/hardware/qualify\_writable\_image\.yml \- pass the full connection options when re\-reading a media session\'s byte counters\. The call omitted <code>tls\_fingerprint</code> and the redirection port\, so it failed with <code>error\_class\=tls\_validation</code> after the writable\-device assertion had already passed\.
* tests/hardware/render\-inventory\.sh \- emit credentials as <code>lookup\(\'ansible\.builtin\.env\'\, \.\.\.\)</code> expressions instead of literal values\, so the AMT password and TLS fingerprint never touch disk\. Also accept <code>AMT\_HOST</code>\, <code>AMT\_PORT</code> and <code>AMT\_REDIRECTION\_PORT</code>\, matching the variables the lab context provides\.
* tests/hardware/render\-inventory\.sh \- emit the <code>amt\_targets</code> group the qualification playbooks actually target\. It previously emitted <code>amt\_lab</code>\, so every play would have matched zero hosts and reported success having done nothing\.
* tests/integration/mock\_servers/wsman\_server\.py \- AMT\_BootCapabilities now answers Enumerate as well as Get\. amt\_boot\'s discover\_and\_validate\(\) and redirection\_service\.py\'s get\_capabilities\(\) both reach it via Enumerate\+Pull \(it has no natural instance key for a Get SelectorSet\)\; the mock only implemented the Get form used by amt\_info\'s facts\-gathering path\, which every prior unit test masked by mocking WsmanClient\.enumerate\(\) directly instead of exercising this server \([https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/8](https\://github\.com/james\-crowley/ansible\-collection\-intel\-amt/issues/8)\)\.

<a id="new-modules"></a>
### New Modules

* james\_crowley\.intel\_amt\.amt\_boot \- Arm a one\-time boot device selection on an Intel AMT endpoint
* james\_crowley\.intel\_amt\.amt\_info \- Gather Intel AMT capability and state facts
* james\_crowley\.intel\_amt\.amt\_media \- Attach or detach IDE\-R virtual media \(bootable ISO and/or writable image\)
* james\_crowley\.intel\_amt\.amt\_power \- Control and query Intel AMT power state
* james\_crowley\.intel\_amt\.amt\_redirection \- Report and optionally toggle Intel AMT redirection\-service enablement
